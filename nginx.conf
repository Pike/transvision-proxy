events {
    worker_connections  1024;
}

error_log /app/logs/nginx-error.log;
daemon off;

http{
    server {
        listen 8080;
        location / {
            proxy_pass http://transvision.mozfr.org/;
        }
    }
}